# Uncomment the next line to define a global platform for your project
platform :ios, '13.2'

target 'camera0320' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod "GPUImage"
end
  # Pods for camera0320

  target 'camera0320Tests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'camera0320UITests' do
    # Pods for testing
  end
